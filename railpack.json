{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "build": {
      "commands": ["...", "pnpm build:api"]
    }
  },
  "deploy": {
    "startCommand": "pnpm start"
  }
}
